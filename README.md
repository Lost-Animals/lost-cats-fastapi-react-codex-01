# lost-cats-fastapi-react-codex-01

## Backend (FastAPI)

Set the database URL and run the API server:

```bash
export DATABASE_URL="postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME"
uvicorn backend.app.main:app --reload
```
